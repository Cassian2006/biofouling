# API 设计草案

## 目标

API 首版服务于三个用途：

1. 健康检查
2. 单船报告请求与结果返回
3. 区域风险图层查询

## 建议接口

### `GET /health`

返回服务状态与当前版本。

### `POST /reports/voyage`

输入船舶标识与时间范围，触发单船航次级报告生成。

### `GET /layers/regional-risk`

返回研究区某时间窗内的区域风险图层或索引结果。

## 当前约束

第一阶段先保证接口结构存在，具体任务调度、异步执行和缓存机制后续再补。

